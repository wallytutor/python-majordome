[project]
name = "majordome-calphad"
version = "0.1.0"
description = "Crate providing Calphad models to the Majordome stack."
readme = "README.md"
authors = [
    { name = "Walter Dal'Maz Silva", email = "walter.dalmazsilva@gmail.com" },
]
requires-python = ">=3.12"
dependencies = ["majordome-build"]

[project.optional-dependencies]
docs = [
    "ipykernel>=7.3.0",
    "sympy>=1.14.0",
]

[tool.maturin]
module-name = "majordome_calphad.calphad"
python-packages = ["majordome_calphad"]
python-source = "src"

[tool.uv]
cache-keys = [
    { file = "pyproject.toml" },
    { file = "src/**/*.rs" },
    { file = "Cargo.toml" },
    { file = "Cargo.lock" },
]

[tool.uv.sources]
majordome-build = { git = "https://github.com/wallytutor/majordome-build.git" }

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"
